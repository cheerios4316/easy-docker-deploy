export enum Commands {
    DEPLOY = "deploy",
    RESTART = "restart",
    BUILD = "build",
}

export enum ArgumentNames {
    PROJECT = "project"
}

export enum CommandStages {
    PULL = "pull",
    BUILD = "build",
    START = "start",
    CD = "cd",
    RESTART = "restart",
}